"""Compatibility imports for canonical lease-protocol constants."""

try:
    from ..._shared.protocol.constants import (
        _METHOD_RE,
        _NONCE_RE,
        _PROCESS_STARTED_AT,
        _PROOF_RE,
        _REDACTED,
        _REQUEST_PROOF_DOMAIN,
        _RESPONSE_PROOF_DOMAIN,
        _SAFE_IDENTIFIER_RE,
        _SENSITIVE_KEYS,
        _TOKEN_RE,
        DEFAULT_REPLAY_RESPONSE_MAX_BYTES,
        DEFAULT_REPLAY_TTL_SECONDS,
        DEFAULT_SESSION_TTL_SECONDS,
        HANDSHAKE_REQUEST_KIND,
        HANDSHAKE_RESPONSE_KIND,
        HMAC_ALGORITHM,
        MAX_ENVELOPE_BYTES,
        MAX_HANDSHAKE_BYTES,
        MAX_HANDSHAKE_NONCES,
        MAX_LEASE_CREDENTIALS,
        MAX_PARAMS_DEPTH,
        MAX_SECRET_FILE_BYTES,
        MAX_SESSION_TTL_SECONDS,
        MIN_SECRET_BYTES,
        PROTOCOL_NAME,
        PROTOCOL_VERSION,
        REQUIRED_PROTOCOL_FEATURES,
        SUPPORTED_FEATURES,
    )
except ImportError:
    from _shared.protocol.constants import (
        _METHOD_RE,
        _NONCE_RE,
        _PROCESS_STARTED_AT,
        _PROOF_RE,
        _REDACTED,
        _REQUEST_PROOF_DOMAIN,
        _RESPONSE_PROOF_DOMAIN,
        _SAFE_IDENTIFIER_RE,
        _SENSITIVE_KEYS,
        _TOKEN_RE,
        DEFAULT_REPLAY_RESPONSE_MAX_BYTES,
        DEFAULT_REPLAY_TTL_SECONDS,
        DEFAULT_SESSION_TTL_SECONDS,
        HANDSHAKE_REQUEST_KIND,
        HANDSHAKE_RESPONSE_KIND,
        HMAC_ALGORITHM,
        MAX_ENVELOPE_BYTES,
        MAX_HANDSHAKE_BYTES,
        MAX_HANDSHAKE_NONCES,
        MAX_LEASE_CREDENTIALS,
        MAX_PARAMS_DEPTH,
        MAX_SECRET_FILE_BYTES,
        MAX_SESSION_TTL_SECONDS,
        MIN_SECRET_BYTES,
        PROTOCOL_NAME,
        PROTOCOL_VERSION,
        REQUIRED_PROTOCOL_FEATURES,
        SUPPORTED_FEATURES,
    )

__all__ = [
    "DEFAULT_REPLAY_RESPONSE_MAX_BYTES",
    "DEFAULT_REPLAY_TTL_SECONDS",
    "DEFAULT_SESSION_TTL_SECONDS",
    "HANDSHAKE_REQUEST_KIND",
    "HANDSHAKE_RESPONSE_KIND",
    "HMAC_ALGORITHM",
    "MAX_ENVELOPE_BYTES",
    "MAX_HANDSHAKE_BYTES",
    "MAX_HANDSHAKE_NONCES",
    "MAX_LEASE_CREDENTIALS",
    "MAX_PARAMS_DEPTH",
    "MAX_SECRET_FILE_BYTES",
    "MAX_SESSION_TTL_SECONDS",
    "MIN_SECRET_BYTES",
    "PROTOCOL_NAME",
    "PROTOCOL_VERSION",
    "REQUIRED_PROTOCOL_FEATURES",
    "SUPPORTED_FEATURES",
    "_METHOD_RE",
    "_NONCE_RE",
    "_PROCESS_STARTED_AT",
    "_PROOF_RE",
    "_REDACTED",
    "_REQUEST_PROOF_DOMAIN",
    "_RESPONSE_PROOF_DOMAIN",
    "_SAFE_IDENTIFIER_RE",
    "_SENSITIVE_KEYS",
    "_TOKEN_RE",
]
