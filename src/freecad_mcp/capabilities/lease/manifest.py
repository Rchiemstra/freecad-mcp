"""Capability manifest for lease (bootstrapped)."""

# ruff: noqa: E501
from __future__ import annotations

from ..schema import ExecutionMode, MutationClass, SubjectManifest, ToolEntry

MANIFEST = SubjectManifest(
    subject="lease",
    register_modules=('tools_lease_acquire_a', 'tools_lease_acquire_b', 'tools_lease_lifecycle'),
    tools=(
        ToolEntry(
            name="acquire_document_lock",
            docstring='Acquire an exclusive renewable write lease for a FreeCAD document.\n\nPrefer ``selector`` with an addon-issued ``document_session_uuid`` plus\noptional name/path assertions. All supplied selector fields must resolve to\nthe same open document; ActiveDocument is never used. The one-time private\ncredential is retained by this MCP process for later calls.\n\n``doc_name``, ``file_path``, and ``session_id`` are deprecated selector\naliases. They still acquire through the authenticated protocol-v2 service\nand produce the same canonical schema-v2 sidecar as ``selector``.',
            signature='(ctx: \'Context\', doc_name: \'str\' = \'\', file_path: \'str\' = \'\', session_id: \'str\' = \'\', task_description: \'str\' = \'\', selector: \'DocumentSelectorInput | None\' = None, agent_id: \'str\' = \'\', hash_policy: "Literal[\'sha256\']" = \'sha256\') -> \'CallToolResult\'',
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="acquire_document_lock",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_a",
        ),
        ToolEntry(
            name="adopt_dirty_document",
            docstring="Adopt existing unsaved changes into the verified lease-v2 lifecycle.\n\nThe selector must contain ``document_name``, ``document_session_uuid``, or\n``canonical_path``. Initial adoption of an unlocked dirty document is\nauto-authorized (no FreeCAD pop-up). Taking over another agent's dirty\n``LOCKED_ERROR`` lease is also auto-authorized without a FreeCAD pop-up.\nThe bounded handoff runs asynchronously while the tool returns a non-error\n``LOCKED_ERROR_HANDOFF_PENDING`` result with a ``request_id``. Resume with\n``get_request_status`` then\n``claim_acquisition_result`` (same path as transport loss).\n``cancel_request`` aborts the handoff before CAS. Adoption creates a\nrecovery snapshot before this MCP process receives the lease credential.\nThe main FCStd is not saved by adoption.",
            signature='(ctx: \'Context\', selector: \'DocumentSelectorInput\', task_description: \'str\' = \'\', agent_id: \'str\' = \'\', hash_policy: "Literal[\'sha256\']" = \'sha256\') -> \'CallToolResult\'',
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="adopt_dirty_document",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_a",
        ),
        ToolEntry(
            name="get_document_lock",
            docstring='Return redacted effective local/foreign lease state for one document.\n\nPrefer ``selector``. The legacy identity arguments remain temporarily for\noff/observe migration compatibility. Status never includes the bearer token\nor its fingerprint; malformed or conflicting sidecars report locked/unknown.',
            signature="(ctx: 'Context', doc_name: 'str' = '', file_path: 'str' = '', session_id: 'str' = '', selector: 'DocumentSelectorInput | None' = None) -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="get_document_lock",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_a",
        ),
        ToolEntry(
            name="list_document_locks",
            docstring='List redacted local, foreign, stale, error, and dirty-recovery records.\n\nThe response contains no bearer tokens or token fingerprints.',
            signature="(ctx: 'Context') -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="list_document_locks",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_a",
        ),
        ToolEntry(
            name="release_document_lock",
            docstring='CAS-release only a clean, verified saved/restored document lease.\n\nPrefer ``selector``; its credential is selected from private MCP memory.\n``doc_key`` and ``token`` are deprecated protocol-v1 compatibility fields\nfor off/observe migration only and are rejected for enforce-mode mutation.\nAgents cannot use this tool for dirty abandonment.',
            signature='(ctx: \'Context\', selector: \'DocumentSelectorInput | None\' = None, disposition: "Literal[\'saved\', \'restored\']" = \'saved\', doc_key: \'str\' = \'\', token: \'str\' = \'\') -> \'CallToolResult\'',
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="release_document_lock",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_b",
        ),
        ToolEntry(
            name="update_document_lock",
            docstring='Update bounded task/progress metadata without changing authority.\n\nState, heartbeat, dirty status, errors, and mutation revisions remain\nserver-owned and cannot be supplied through this tool.',
            signature="(ctx: 'Context', selector: 'DocumentSelectorInput', task_description: 'str' = '', progress_detail: 'str' = '') -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.legacy_shims.legacy_removed_tool",
            rpc_method="update_document_lock",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_acquire_b",
        ),
        ToolEntry(
            name="finalize_document_edit",
            docstring='Run native change-aware Save/Save As, verify a stable live FCStd hash,\nmetadata, required archive members, and CRC, then report finalization.\n\nNo legacy MCP lease is held or released. A save, clean-state, or verification\nfailure returns ``finalized: false`` and keeps automation fail-closed.',
            signature='(ctx: \'Context\', selector: \'DocumentSelectorInput\', save_mode: "Literal[\'save\', \'save_as\', \'first_save\']" = \'save\', destination: \'str\' = \'\', overwrite: \'bool\' = False, expected_destination_sha256: \'str\' = \'\', validation_profile: \'str\' = \'default\') -> \'CallToolResult\'',
            operation_path="freecad_mcp.capabilities.gateway_refs.connection:finalize_document_edit",
            rpc_method="finalize_document_edit",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_lifecycle",
        ),
        ToolEntry(
            name="save_document",
            docstring='Run native change-aware Save and verify a stable live FCStd hash, metadata,\nrequired archive members, and CRC. An Unchanged result is also compared with\nthe invocation baseline and does not rewrite the file.\n\nSelect the open document with ``document_name`` or ``canonical_path``; if both\nare supplied, they must identify the same live document.',
            signature="(ctx: 'Context', selector: 'DocumentSelectorInput', validation_profile: 'str' = 'default') -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.gateway_refs.connection:save_document",
            rpc_method="save_document",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_lifecycle",
        ),
        ToolEntry(
            name="save_document_as",
            docstring='Run native policy-controlled Save As (no-clobber by default) and verify a\nstable live FCStd hash, metadata, required archive members, and CRC. No legacy\nlease aliases are created.\n\nSelect the open document with ``document_name`` or ``canonical_path``; if both\nare supplied, they must identify the same live document. With ``overwrite=True``,\na non-empty ``expected_destination_sha256`` enables native conflict-safe compare-\nand-swap: the destination is replaced only when its verified SHA-256 matches.',
            signature="(ctx: 'Context', selector: 'DocumentSelectorInput', destination: 'str', overwrite: 'bool' = False, expected_destination_sha256: 'str' = '', validation_profile: 'str' = 'default') -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.gateway_refs.connection:save_document_as",
            rpc_method="save_document_as",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_lifecycle",
        ),
        ToolEntry(
            name="save_document_copy",
            docstring='Run native Save Copy via ``saveCopyWithOutcome`` and verify a stable live\nFCStd hash, metadata, required archive members, and CRC. The canonical\nsavepoint must not move and pending-file state must remain intact.\n\nSave Copy remains available as an extraction path for quarantined or poisoned\ndocuments. Such a result is marked ``recovery_only=true`` and\n``model_state_verified=false``: archive structure and storage durability are\nverified, but the model must be inspected after reopening and the operation\ndoes not clear quarantine.\n\nSelect the open document with ``document_name`` or ``canonical_path``; if both\nare supplied, they must identify the same live document. ``overwrite=False``\nrefuses an existing destination.',
            signature="(ctx: 'Context', selector: 'DocumentSelectorInput', destination: 'str', overwrite: 'bool' = False, validation_profile: 'str' = 'default') -> 'CallToolResult'",
            operation_path="freecad_mcp.capabilities.gateway_refs.connection:save_document_copy",
            rpc_method="save_document_copy",
            execution_mode=ExecutionMode.TYPED_GATEWAY,
            gui_thread=False,
            mutation_class=MutationClass.LEASE,
            register_module="tools_lease_lifecycle",
        ),
    ),
)
