"""GENERATED by freecad_mcp.capabilities.generator — do not edit."""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any


def _recover_acquisition_after_transport_loss(
    conn,
    method: str,
    request_id: str,
    *,
    params: Mapping[str, Any] | None = None,
    poll_attempts: int = 8,
    poll_interval_s: float = 0.25,
) -> None:
    """Never recover or claim document authority after transport loss."""

    del conn, method, request_id, params, poll_attempts, poll_interval_s


def _resolve_locked_error_handoff_pending(
    conn,
    result: Mapping[str, Any],
    *,
    poll_interval_s: float = 0.5,
    max_wait_s: float | None = None,
) -> dict[str, Any]:
    """Return the frozen deprecation for the removed handoff authority."""

    del conn, poll_interval_s, max_wait_s
    if result.get("error_code") != "LOCKED_ERROR_HANDOFF_PENDING":
        return dict(result)
    return {
        "success": False,
        "ok": False,
        "error_code": "LEGACY_LEASE_AUTHORITY_REMOVED",
        "error": "Document authority is owned by native FreeCAD collaboration.",
    }

