"""GENERATED by freecad_mcp.capabilities.generator — do not edit."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from mcp.server.fastmcp import Context
from mcp.types import CallToolResult

from freecad_mcp.operations import (
    encode_view_video_operation,
    get_view_operation,
    save_view_sequence_operation,
)
from freecad_mcp.server_ops.tool_dependencies import ToolDependencies
from freecad_mcp.tools_server_surfaces import server_connection, server_state

if TYPE_CHECKING:
    from freecad_mcp.instrumented_server import InstrumentedFastMCP
def _register_get_view(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def get_view(
        ctx: Context,
        view_name: Literal[
            "Isometric",
            "Front",
            "Top",
            "Right",
            "Back",
            "Left",
            "Bottom",
            "Dimetric",
            "Trimetric",
            "Rear",
            "Side",
            "SideRight",
            "SideLeft",
        ],
        width: int | None = None,
        height: int | None = None,
        focus_object: str | None = None,
        focus_objects: list[str] | None = None,
        yaw_deg: float | None = None,
    ) -> CallToolResult:
        """Get a screenshot of the active view.

        Args:
            view_name: Standard view preset. Aliases: Rear→Back, Side/SideRight→Right, SideLeft→Left.
            Available: Isometric, Front, Top, Right, Back, Left, Bottom, Dimetric, Trimetric
            (plus Rear, Side, SideRight, SideLeft aliases).
            width: The width of the screenshot in pixels. If not specified, uses the viewport width.
            height: The height of the screenshot in pixels. If not specified, uses the viewport height.
            focus_object: Optional single object name to frame. Comma-separated names are
                also accepted.
            focus_objects: Optional list of object names to frame together (preferred for
                stations/assemblies).
            yaw_deg: Optional extra camera yaw in degrees after framing.

        Returns:
            A screenshot of the active view.
        """
        return get_view_operation(
            server_connection(),
            view_name,
            width,
            height,
            focus_object=focus_object,
            focus_objects=focus_objects,
            yaw_deg=yaw_deg,
        )

    exports['get_view'] = get_view
def _register_save_view_sequence(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def save_view_sequence(
        ctx: Context,
        frames: list[dict[str, Any]] | None = None,
        width: int | None = None,
        height: int | None = None,
        orbit: dict[str, Any] | None = None,
    ) -> CallToolResult:
        """Capture multiple framed screenshots for motion/station review.

        Provide ``frames`` and/or ``orbit``.

        Frame dict fields:
        - ``view_name`` (default ``Isometric``)
        - ``focus_object`` / ``focus_objects``
        - ``yaw_deg``
        - ``label``

        Orbit dict fields:
        - ``focus_object`` / ``focus_objects``
        - ``steps`` (default 8)
        - ``view_name`` (default ``Isometric``)
        - ``yaw_start_deg`` (default 0)

        Returns text metadata plus one PNG image per successful frame.
        """
        return save_view_sequence_operation(
            server_connection(),
            frames=frames,
            width=width,
            height=height,
            orbit=orbit,
            only_text_feedback=server_state().only_text_feedback,
        )

    exports['save_view_sequence'] = save_view_sequence
def _register_encode_view_video(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def encode_view_video(
        ctx: Context,
        frames: list[dict[str, Any]] | None = None,
        orbit: dict[str, Any] | None = None,
        frame_paths: list[str] | None = None,
        output_path: str | None = None,
        fps: float = 8.0,
        width: int | None = None,
        height: int | None = None,
    ) -> CallToolResult:
        """Encode a view sequence to MP4 using system ffmpeg (not a git submodule).

        Provide existing ``frame_paths`` and/or capture via ``frames``/``orbit``
        (same shape as ``save_view_sequence``). Resolves ffmpeg from
        ``FREECAD_MCP_FFMPEG`` or PATH.
        """
        return encode_view_video_operation(
            server_connection(),
            frames=frames,
            orbit=orbit,
            frame_paths=frame_paths,
            output_path=output_path,
            fps=fps,
            width=width,
            height=height,
            only_text_feedback=server_state().only_text_feedback,
        )

    exports['encode_view_video'] = encode_view_video

def register(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
) -> dict[str, object]:
    """Register gui_view_a MCP tools; return exports for §3.3 façade shims."""
    exports: dict[str, object] = {}
    _register_get_view(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_save_view_sequence(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_encode_view_video(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    return exports

