"""GENERATED by freecad_mcp.capabilities.generator — do not edit."""

from __future__ import annotations
from typing import TYPE_CHECKING, Any, Literal

from mcp.server.fastmcp import Context
from mcp.types import CallToolResult

from freecad_mcp.operations import (
    create_assembly_grounded_joint_operation,
    create_assembly_joint_operation,
    create_assembly_operation,
    solve_assembly_operation,
)
from freecad_mcp.server_ops.tool_dependencies import ToolDependencies
from freecad_mcp.tools_server_surfaces import server_connection, server_state

if TYPE_CHECKING:
    from freecad_mcp.instrumented_server import InstrumentedFastMCP
def _register_create_assembly(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def create_assembly(
        ctx: Context,
        doc_name: str,
        assembly_name: str = "Assembly",
        create_joint_group: bool = True,
        recompute: bool = False,
        if_exists: Literal["error", "skip", "replace"] = "error",
    ) -> CallToolResult:
        """Create a built-in Assembly workbench assembly object."""
        return create_assembly_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            assembly_name,
            create_joint_group,
            recompute,
            if_exists,
        )

    exports['create_assembly'] = create_assembly
def _register_create_assembly_grounded_joint(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def create_assembly_grounded_joint(
        ctx: Context,
        doc_name: str,
        assembly_name: str,
        component_name: str,
        label: str | None = None,
        recompute: bool = True,
    ) -> CallToolResult:
        """Ground an assembly component through the headless Assembly API."""
        return create_assembly_grounded_joint_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            assembly_name,
            component_name,
            label,
            recompute,
        )

    exports['create_assembly_grounded_joint'] = create_assembly_grounded_joint
def _register_create_assembly_joint(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def create_assembly_joint(
        ctx: Context,
        doc_name: str,
        assembly_name: str,
        joint_type: Literal[
            "Fixed",
            "Revolute",
            "Cylindrical",
            "Slider",
            "Ball",
            "Distance",
            "Parallel",
            "Perpendicular",
            "Angle",
            "RackPinion",
            "Screw",
            "Gears",
            "Belt",
        ],
        ref1_component: str,
        ref2_component: str,
        ref1_element: str = "",
        ref2_element: str = "",
        ref1_vertex: str | None = None,
        ref2_vertex: str | None = None,
        label: str | None = None,
        solve: bool = True,
        presolve: bool = True,
        recompute: bool = True,
        properties: dict[str, Any] | None = None,
    ) -> CallToolResult:
        """Create a built-in Assembly joint from two component subelement references."""
        return create_assembly_joint_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            assembly_name,
            joint_type,
            ref1_component,
            ref2_component,
            ref1_element,
            ref2_element,
            ref1_vertex,
            ref2_vertex,
            label,
            solve,
            presolve,
            recompute,
            properties,
        )

    exports['create_assembly_joint'] = create_assembly_joint
def _register_solve_assembly(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def solve_assembly(ctx: Context, doc_name: str, assembly_name: str) -> CallToolResult:
        """Re-solve an Assembly after editing a joint or a referenced face (I9 / P9).

        Tries ``assembly.solve()`` (C++), then ``JointObject.solveIfAllowed``, then a
        plain recompute, and reports which method succeeded. Returns JSON
        ``{ok, assembly, method, status}`` plus a screenshot.

        Args:
            doc_name: The document containing the assembly.
            assembly_name: The name of the Assembly::AssemblyObject to solve.
        """
        return solve_assembly_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            assembly_name,
        )

    exports['solve_assembly'] = solve_assembly

def register(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
) -> dict[str, object]:
    """Register assembly MCP tools; return exports for §3.3 façade shims."""
    exports: dict[str, object] = {}
    _register_create_assembly(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_create_assembly_grounded_joint(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_create_assembly_joint(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_solve_assembly(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    return exports

