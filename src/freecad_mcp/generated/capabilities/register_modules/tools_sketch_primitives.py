"""GENERATED by freecad_mcp.capabilities.generator — do not edit."""

from __future__ import annotations
from typing import TYPE_CHECKING

from mcp.server.fastmcp import Context
from mcp.types import CallToolResult

from freecad_mcp.operations import (
    sketch_add_arc_operation,
    sketch_add_circle_operation,
    sketch_add_line_operation,
    sketch_add_rectangle_operation,
)
from freecad_mcp.server_ops.tool_dependencies import ToolDependencies
from freecad_mcp.tools_server_surfaces import server_connection, server_state

if TYPE_CHECKING:
    from freecad_mcp.instrumented_server import InstrumentedFastMCP
def _register_sketch_add_line(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def sketch_add_line(
        ctx: Context,
        doc_name: str,
        sketch_name: str,
        x1: float,
        y1: float,
        x2: float,
        y2: float,
        construction: bool = False,
    ) -> CallToolResult:
        """Add a line segment to a sketch.

        All coordinates are in the sketch's local 2-D plane (mm).

        Args:
            doc_name: Document containing the sketch.
            sketch_name: Name of the target sketch.
            x1: X coordinate of the start point.
            y1: Y coordinate of the start point.
            x2: X coordinate of the end point.
            y2: Y coordinate of the end point.
            construction: If true, add as a construction (helper) line.

        Returns:
            Success message with the assigned geometry index and a screenshot.
        """
        return sketch_add_line_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            sketch_name,
            x1,
            y1,
            x2,
            y2,
            construction,
        )

    exports['sketch_add_line'] = sketch_add_line
def _register_sketch_add_circle(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def sketch_add_circle(
        ctx: Context,
        doc_name: str,
        sketch_name: str,
        cx: float,
        cy: float,
        radius: float,
        construction: bool = False,
    ) -> CallToolResult:
        """Add a full circle to a sketch.

        Args:
            doc_name: Document containing the sketch.
            sketch_name: Name of the target sketch.
            cx: X coordinate of the centre.
            cy: Y coordinate of the centre.
            radius: Circle radius in mm.
            construction: If true, add as a construction circle.

        Returns:
            Success message with the assigned geometry index and a screenshot.
        """
        return sketch_add_circle_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            sketch_name,
            cx,
            cy,
            radius,
            construction,
        )

    exports['sketch_add_circle'] = sketch_add_circle
def _register_sketch_add_arc(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def sketch_add_arc(
        ctx: Context,
        doc_name: str,
        sketch_name: str,
        cx: float,
        cy: float,
        radius: float,
        start_angle: float,
        end_angle: float,
        construction: bool = False,
    ) -> CallToolResult:
        """Add a circular arc to a sketch.

        Angles are in degrees, measured counter-clockwise from the positive X axis.

        Args:
            doc_name: Document containing the sketch.
            sketch_name: Name of the target sketch.
            cx: X coordinate of the arc centre.
            cy: Y coordinate of the arc centre.
            radius: Arc radius in mm.
            start_angle: Start angle in degrees (0 = right, 90 = up).
            end_angle: End angle in degrees (must be > start_angle for CCW arc).
            construction: If true, add as a construction arc.

        Returns:
            Success message with the assigned geometry index and a screenshot.
        """
        return sketch_add_arc_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            sketch_name,
            cx,
            cy,
            radius,
            start_angle,
            end_angle,
            construction,
        )

    exports['sketch_add_arc'] = sketch_add_arc
def _register_sketch_add_rectangle(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
    exports: dict[str, object],
) -> None:
    @mcp.tool()
    def sketch_add_rectangle(
        ctx: Context,
        doc_name: str,
        sketch_name: str,
        x1: float,
        y1: float,
        x2: float,
        y2: float,
        construction: bool = False,
    ) -> CallToolResult:
        """Add an axis-aligned rectangle to a sketch (4 connected line segments).

        Returns the 4 geometry indices in order: bottom, right, top, left.

        Args:
            doc_name: Document containing the sketch.
            sketch_name: Name of the target sketch.
            x1: X coordinate of the first corner.
            y1: Y coordinate of the first corner.
            x2: X coordinate of the opposite corner.
            y2: Y coordinate of the opposite corner.
            construction: If true, add all edges as construction lines.

        Returns:
            Success message with the 4 assigned geometry indices and a screenshot.
        """
        return sketch_add_rectangle_operation(
            server_connection(),
            server_state().only_text_feedback,
            doc_name,
            sketch_name,
            x1,
            y1,
            x2,
            y2,
            construction,
        )

    exports['sketch_add_rectangle'] = sketch_add_rectangle

def register(
    mcp: InstrumentedFastMCP,
    *,
    dependencies: ToolDependencies,
) -> dict[str, object]:
    """Register sketch_primitives MCP tools; return exports for §3.3 façade shims."""
    exports: dict[str, object] = {}
    _register_sketch_add_line(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_sketch_add_circle(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_sketch_add_arc(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    _register_sketch_add_rectangle(
        mcp,
        dependencies=dependencies,
        exports=exports,
    )
    return exports

